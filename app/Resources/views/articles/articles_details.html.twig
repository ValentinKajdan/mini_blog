{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>{{ article.title }}</h1>
        <h3>{{ article.author }}</h3>
        {{ article.content }}

    </div>
{% endblock %}
