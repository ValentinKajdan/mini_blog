{% extends 'base.html.twig' %}

{% block title %}Catégorie {{ category.title }}{% endblock %}

{% block body %}
  <div class="container">
    {{ category.title }}
    Les articles :
    {% for article in articles %}
        <li>
          <a href="{{ path('articles_name', {'id': article.id}) }}">
            {{ article.title }} par {{ article.author }}
          </a>
        </li>
    {% endfor %}
  </div>

{% endblock %}
